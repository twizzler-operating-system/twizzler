[package]
name = "lwext4-rs"
version = "0.1.0"
edition = "2021"

[dependencies]
twizzler-abi = { path = "../../lib/twizzler-abi" }
naming-core = { path = "../../lib/naming/naming-core" }


[build-dependencies]
