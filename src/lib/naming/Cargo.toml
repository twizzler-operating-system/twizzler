[package]
name = "naming"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["rlib"]

[dependencies]
naming-srv = { path = "../../srv/naming-srv" }
secgate = { path = "../../lib/secgate" }
twizzler-rt-abi = "0.99"
