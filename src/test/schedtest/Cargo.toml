[package]
name = "st"
version = "0.1.0"
edition = "2024"

[dependencies]

[target.'cfg(target_os = "twizzler")'.dependencies]
twizzler-abi = { path = "../../lib/twizzler-abi" }

[package.metadata]
twizzler-build = "test"
